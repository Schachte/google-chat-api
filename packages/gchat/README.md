# gchat-cli

See the [root README](../../README.md) for full docs.

```bash
npm install && npm run build
npm link    # puts gchat on your PATH
```

### Dev

```bash
npm run dev              # CLI with hot reload
npm run serve:watch      # API server with hot reload
npm run bridge:watch     # bridge with hot reload
npm test
```
